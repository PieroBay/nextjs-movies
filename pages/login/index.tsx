import type {NextPage} from 'next'
import {Typography} from "@mui/material";

const Login: NextPage = () => {
    return <div>
        <Typography variant="h1">Login work!</Typography>
    </div>
}

export default Login
